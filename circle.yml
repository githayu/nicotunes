machine:
  timezone: Asia/Tokyo
  node:
    version: 5.11.0

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install wine
  override:
    - npm i
    - npm i -D request
  post:
    - npm run init

deployment:
  master:
    branch: master
    commands:
      - ./scripts/release.sh
